#!/bin/sh

shell_root=$(CDPATH= cd -- "$(dirname -- "$0")/../quickshell" && pwd)
summary=${1:-"Transit link online"}
body=${2:-"This toast is local to the active Tonantzintla shell."}

exec qs -p "$shell_root" ipc call transit preview "$summary" "$body"
